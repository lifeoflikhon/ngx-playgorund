import { GetColumnPipe } from './get-column.pipe';

describe('GetColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new GetColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
